#listdir() returns a list of files and folders present in the current directory.
#  use getcwd() to get the current working directory
import os
current_directory = os.getcwd()
print(current_directory)